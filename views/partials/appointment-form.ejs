<div class="form-field">
  <label for="title">Title:</label>
  <input type="text" id="title" name="title" value="<%= currentPage === 'edit' ? appointment.title : '' %>" required>
</div>
<div class="form-field">
  <label for="date">Date:</label>
  <input type="date" id="date" name="date" value="<%= currentPage === 'edit' ? appointment.date : '' %>" required>
</div>
<div class="form-field">
  <label for="desc">Description:</label>
  <textarea id="desc" name="desc" required><%= currentPage === 'edit' ? appointment.desc : '' %></textarea>
</div>
<div class="form-field">
  <label for="time">Time:</label>
  <input type="time" id="time" name="time" value="<%= currentPage === 'edit' ? appointment.time : '' %>" required>
</div>
<div class="form-field">
  <label for="location">Location:</label>
  <input type="text" id="location" name="location" value="<%= currentPage === 'edit' ? appointment.location : '' %>" required>
</div>
<div class="form-field">
  <label for="note">Notes:</label>
  <textarea id="note" name="note"><%= currentPage === 'edit' ? appointment.note : '' %></textarea>
</div>